# Markdown and JavaScript in MDX

This is a simple example of using Markdown and JavaScript in MDX.

```javascript
// Execute JavaScript code within MDX
const message = 'Hello, MDX!';
console.log(message);